syntax = "proto3";

package microservices_starter.users.auth;

import "../common/event.proto"; 

message UserCreatedEvent {
  string id = 1;
  string email = 2;
  string first_name = 3;
  string last_name = 4;

  EventMeta metadata = 5;
}
