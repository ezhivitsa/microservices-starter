export enum Media {
  Small = '--small',
  SmallOnly = '--small-only',
  Medium = '--medium',
  MediumOnly = '--medium-only',
  Large = '--large',
  LargeOnly = '--large-only',
  XLarge = '--xlarge',
  XLargeOnly = '--xlarge-only',
  XXLarge = '--xxlarge',
  XXLargeOnly = '--xxlarge-only',
}
