@import 'variables.pcss';

.message {
  &__icon-wrap {
    display: flex;
    padding: var(--grid2);
    align-items: center;
  }

  &__header {
    color: 000;
  }

  &__content {
    color: 000;
  }
}
