@import 'variables.pcss';

.form {
  padding: var(--grid4);
  border-radius: var(--border-radius-400);
  max-width: 500px;
  margin: auto;
}

.form_theme_light {
  border: 1px solid var(--color-grey-400);
}
